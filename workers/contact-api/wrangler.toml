name = "portfolio-contact-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# Public EmailJS identifiers (not secrets). Private key must be stored as a Worker secret.
EMAILJS_PUBLIC_KEY = "0angy-FFhuMJiDUal"
EMAILJS_SERVICE_ID = "service_c4ug6xj"
EMAILJS_TEMPLATE_ID = "template_o7kynn5"
ALLOWED_ORIGINS = "https://theshoaib.me,https://www.theshoaib.me,https://asmi-bae.github.io,https://www.asmi-bae.github.io"

# Create once: npx wrangler kv namespace create DOWNLOAD_STATS
# Then paste the returned id below (and preview_id for local dev).
[[kv_namespaces]]
binding = "DOWNLOAD_STATS"
id = "f9ce9ac547764f7580cf49b088f2796d"
preview_id = "58613de46fc1434db779d9e043e8326e"

# Set secrets with:
# npx wrangler secret put TURNSTILE_SECRET_KEY
# npx wrangler secret put EMAILJS_PRIVATE_KEY
# npx wrangler secret put TELEGRAM_BOT_TOKEN
# npx wrangler secret put TELEGRAM_CHAT_ID
